# E2B (Required for code execution)
# Get key from https://e2b.dev
E2B_API_KEY=

# Google Cloud Storage
NEXT_PUBLIC_GCS_BUCKET_NAME=agent_codes

# GCS credentials – use ONE of:
# Vercel: paste full service account JSON as one line
GOOGLE_APPLICATION_CREDENTIALS_JSON={"type":"service_account",...}
# Local: path to key file (optional)
# GOOGLE_APPLICATION_CREDENTIALS=./path/to/service-account.json
